# Example environment variables for Pantrio
# Free mode (default): keep USE_OPENAI=false and no API key is needed.
# Paid mode (optional): set USE_OPENAI=true and provide OPENAI_API_KEY.

USE_OPENAI=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
